# svgfoldericonindex
Given a folder tree of SVG icons, makes an index of them

## How to use

From the root of the folder tree containing the SVG files

```powershell
.\make_index.ps1
```

### Customize Template

You can Customize the `Template.html`
